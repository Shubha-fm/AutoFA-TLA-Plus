# AutoFA-TLA+ Frontend

React + Vite frontend for drawing or entering DFA/NFA transition tables and converting them into TLA+.

## Run

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173`.

Set API URL if needed:

```bash
VITE_API_URL=http://localhost:8000 npm run dev
```
